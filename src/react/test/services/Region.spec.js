/* global describe, it */
import { expect } from 'chai';

import RegionService from '../../services/Region';

describe('react › services › Region', function () {
  it('is a function', function () {
    expect(RegionService).to.be.a('function');
  });
});
