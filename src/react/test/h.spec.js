/* global describe, it */
import { expect } from 'chai';

import h from '../h';

describe('react › h', function () {
  it('is a function', function () {
    expect(h).to.be.a('function');
  });
});
